
/*
import { Action } from '@ngrx/store';


export function reducer(  state: boolean = true,  action: Action) {
  
  console.log(action.type, state);

  switch (action.type) {
    case 'Show':
      return state = false;
    case 'Hide':
      return state = true;
    default:
      return state;
  }
}
*/
